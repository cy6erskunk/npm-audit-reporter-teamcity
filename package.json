{
  "name": "npm-audit-reporter-teamcity",
  "version": "0.1.0",
  "description": "generate TeamCity code inspections from the output of `npm audit`",
  "main": "dist/index.js",
  "bin": "bin/index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["teamcity", "audit"],
  "author": "Igor Shevchenko",
  "license": "MIT",
  "dependencies": {
    "teamcity-service-messages": "^0.1.9"
  },
  "devDependencies": {
    "@types/node": "^11.12.2"
  }
}
